# EpiWatch API Environment Variables

# JWT Secret Key - CHANGE THIS IN PRODUCTION!
# Generate a secure secret key using: openssl rand -hex 32
SECRET_KEY=epiwatch-secret-key-change-in-production-2025

# Database Configuration (if using PostgreSQL in production)
# DATABASE_URL=postgresql://user:password@localhost:5432/epiwatch

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:8081,http://localhost:3000
